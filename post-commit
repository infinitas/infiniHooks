#!/usr/bin/env php
<?php

require __DIR__ .'/config.php';

if (empty($config[__FILE__])) {
	exit(0);
}

$exit = 0;

foreach ($config[__FILE__] as $command) {
    $output = array();
    $return = 0;
    exec(__DIR__ . "/$command", $output, $return);
    if ($return != 0) {
        echo implode("\n", $output) . "\n";
		$exit = 1;
    }
}

exit ($exit);
